# 我实现的功能

我实现了link、unlink和stat的调度算法,我采取了以下的方式

1. 在root_inode添加或删除了一个direntry项

2. 通过_fd ，然后查对应进程的table，然后找到file，然后修改了trait提供了stat的返回功能，然后对着构造Stat
 

# 简答作业
## 问题1 （Ch6）
#### 在我们的easy-fs中，root inode起着什么作用？如果root inode中的内容损坏了，会发生什么？

1. root_inode是唯一的根节点，它保存了所有的文件节点，如果它损坏了，我们就找不到文件节点了，也就没办法访问


## 问题2 （Ch7）

1. 举出使用 pipe 的一个实际应用的例子。

    ```shell
       cat | wc -l  
    ```
    通过pipe传递ls查看的结果，然后wc进行计数

2. 如果需要在多个进程间互相通信，则需要为每一对进程建立一个管道，非常繁琐，请设计一个更易用的多进程通信机制。

    可以创建一个消息队列，发送者发送到消息队列里，接受者从里面接受消息。


# 荣誉准则

1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

        群友，关于问答题的证明题

1. 此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

        RISCV汇编指令

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。